server:
  port: 9020

spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/testing?useSSL=false&zeroDateTimeBehavior=CONVERT_TO_NULL&useLegacyDatetimeCode=false&serverTimezone=UTC
    url: jdbc:mysql://localhost:3306/rpg
    username: root
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    showSql: true
    hibernate:
      ddlAuto: create-drop